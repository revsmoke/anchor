## PR #3 Review Triage - 2026-05-15

### Scope
Review-only triage for branch `codex/universal-agent-tool-layer`. Product code was not edited. Local evidence checked in the named files plus related route/test/migration code.

### Summary
Actionable: 1, 2, 3, 5, 6, 7, 8.
Needs pushback / non-blocking cleanup: 4.
Noise: none.

### Findings

| # | Classification | Recommendation | Tests / verification |
|---|---|---|---|
| 1 | Actionable | In `context_history/contexts/2026-05-14_universal-tool-layer-slice1-3-implementation.md`, replace every user-specific absolute workspace path entry in `Files Created/Modified` and `Related Files` with repo-relative paths such as `PLAN.md`, `server/app.js`, and `context_history/contexts/2026-05-14_slice3_qa_review.md`. This matches portability rules and prior PR review expectations. | Run a scoped absolute-path scan against `context_history/contexts/2026-05-14_universal-tool-layer-slice1-3-implementation.md` and expect no matches. Run `git diff --check`. |
| 2 | Actionable | Add a blank line after `### Files Created/Modified` before the markdown table in the same context file. The table renders, but the file violates the local context-summary style and common markdown heading spacing rules. | Re-read the file and run `git diff --check`; if markdown lint is available, run it against that file. |
| 3 | Actionable | In `public/js/app.js`, update `loadOpenSafetyEpisode()` so the `!openAcuteEvent` path always clears stale state: set `activeSafetyEventId = ""`, hide `safetyResolutionForm`, clear `safetyResolutionNoteEl.value`, hide/clear `safetyEventStatusEl`, and hide/clear `safetyEventErrorEl` unless an explicit caller wants to preserve a just-resolved success message. Current code only hides the form when `activeSafetyEventId` is already empty, so a previously active event can leave stale resolve UI after the backend reports no open acute event. | Add/adjust browser coverage in `tests/browser/safety-plan.spec.js`: create or surface an acute event, resolve it, reload/revisit Safety, and assert the resolution form is hidden, the note field is empty, and duplicate resolve is not offered. Run `bun run test:browser -- tests/browser/safety-plan.spec.js`. |
| 4 | Needs pushback | `server/app.js` has a duplicate `todayStateFromBootstrap()` fallback while `server/db.js` has `buildTodayStateFromBootstrap()`. This is real duplication, but it is not a behavioral blocker because production uses `db.getToday()` and the fallback supports minimal fake DBs. If cleaning it up, export `buildTodayStateFromBootstrap` from `server/db.js`, import it in `server/app.js`, and remove the local duplicate; alternatively remove the fallback and update all tests/fakes to implement `getToday`. Prefer the export/import route if the team wants app-level fallback compatibility. | Run `bun test tests/api/pass2-timezone-contract.test.js tests/api/pass3-check-ins.test.js` and `bun run test:browser -- tests/browser/pass3-check-in.spec.js`. |
| 5 | Actionable | In `tests/api/pass2-timezone-contract.test.js`, the stale-anchor route test sends `checkInId: setup.today[0].id`, which is an anchor id, not a quick check-in id. Change it to `checkInId: null` or create a valid check-in for the stale anchor day and pass that id. Since the expected response is `404`, `null` is enough to isolate the stale-anchor guard. | Run `bun test tests/api/pass2-timezone-contract.test.js`. Keep the assertions that the stale anchor remains `scheduled` and no next-day plan is created. |
| 6 | Actionable | In `tests/browser/pass3-check-in.spec.js`, replace the request-count assertion in `loads the Today view through the canonical today endpoint` with a successful response assertion. For example, capture `page.waitForResponse(response => new URL(response.url()).pathname === "/api/today" && response.status() === 200)` before the action that should load Today, then await it and optionally assert the response JSON has `data.session.authenticated === true`. Counting requests can pass on a 401/500. | Run `bun run test:browser -- tests/browser/pass3-check-in.spec.js`. |
| 7 | Actionable | In `db/migrations/018_safety_episodes.sql`, add a foreign key for `safety_events.safety_episode_id` to `safety_episodes(id)`. Recommended migration shape: `alter table safety_events add constraint safety_events_safety_episode_id_fkey foreign key (safety_episode_id) references safety_episodes(id) on delete set null;` guarded for repeatability because Postgres lacks `add constraint if not exists`. Use a `do $$ begin if not exists (...) then alter table ...; end if; end $$;` block. | Add SQL-backed verification to `tests/api/safety-routes.test.js` or a migration-focused test that rejects an invalid `safety_episode_id` and allows the normal acute-event create/resolve flow. Run `TEST_DATABASE_URL=postgres://localhost:5432/<tempdb> bun test tests/api/safety-routes.test.js` and `bun run db:migrate`. |
| 8 | Actionable | `server/db.js` `buildTodayStateFromBootstrap()` hardcodes `safetyStatus: { riskTier: "normal", activeEpisode: null }`, while acute episodes are persisted and enforced through `getActiveSafetyEpisode()`. `GET /api/today` should expose active safety state if it is the canonical daily-state endpoint. Recommended change: have `db.getToday(userId, options)` query `getActiveSafetyEpisode(userId)` and pass that into the Today state builder, returning `riskTier: "acute"` and `activeEpisode` when present; keep `normal/null` when absent. If keeping the helper pure, pass `{ activeSafetyEpisode }` as an option. | Add API coverage in `tests/api/safety-routes.test.js` or `tests/api/pass2-timezone-contract.test.js` for an active episode followed by `GET /api/today` returning `safetyStatus.riskTier === "acute"` and non-null `activeEpisode`. Add browser coverage only if Today UI will visibly react. Run `bun test tests/api/safety-routes.test.js tests/api/pass2-timezone-contract.test.js`. |

### Notes
- Finding 4 is the only one I would not block on unless the PR has an explicit no-duplicate-helper standard. It is a maintainability cleanup with a straightforward fix.
- Findings 3 and 8 are linked: stale client safety state is easier to prevent if canonical Today state includes active episode status.
- No product-code fixes were applied in this triage pass.
