{
  "name": "anchor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun server/index.js",
    "test": "bun test tests/unit tests/api",
    "test:browser": "playwright test",
    "db:migrate": "bun scripts/db-migrate.js",
    "db:seed": "bun scripts/db-seed.js",
    "db:reset": "bun scripts/db-reset.js",
    "check:private-beta": "bun scripts/private-beta-check.js"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
